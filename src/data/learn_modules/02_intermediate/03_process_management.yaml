id: process_management_03
level: intermediate
title: "Process Management"
description: "Learn to monitor and control processes"
estimated_time: 35

lessons:
  - id: lesson_01
    title: "Viewing Processes"
    notes: |
      👁️ Process Monitoring Commands
      
      ps - Process Status:
      ───────────────────
      • ps                - Show your processes
      • ps aux            - Show all processes (detailed)
      • ps -ef            - Show all processes (different format)
      • ps -u username    - Show user's processes
      • ps aux | grep app - Find specific process
      
      top - Interactive monitor:
      ─────────────────────────
      • top               - Real-time process viewer
      • Press 'q' to quit
      • Press 'k' to kill process
      • Press 'M' to sort by memory
      • Press 'P' to sort by CPU
      
      htop - Better top (if installed):
      ────────────────────────────────
      • htop              - Colorful, interactive
      • F9 to kill, F10 to quit
    
    exercises:
      - id: ex_01
        type: command
        description: "List all running processes"
        command: ps aux | head -10
        expected_pattern: "PID"
        hints:
          - "Use: ps aux"
          - "Add | head -10 to see first 10"
        points: 15
      
      - id: ex_02
        type: command
        description: "Find your bash process"
        command: ps aux | grep bash | grep -v grep
        expected_pattern: "bash"
        hints:
          - "Use: ps aux | grep bash"
          - "grep -v grep removes the grep command itself"
        points: 20

  - id: lesson_02
    title: "Managing Processes"
    notes: |
      ⚙️ Process Control
      
      Background Jobs:
      ───────────────
      • command &         - Run in background
      • jobs              - List background jobs
      • fg %1             - Bring job 1 to foreground
      • bg %1             - Resume job 1 in background
      • Ctrl+Z            - Suspend current job
      
      Killing Processes:
      ─────────────────
      • kill PID          - Terminate process (SIGTERM)
      • kill -9 PID       - Force kill (SIGKILL)
      • killall name      - Kill by process name
      • pkill pattern     - Kill by pattern
      
      Process Priority:
      ────────────────
      • nice -n 10 command    - Start with lower priority
      • renice -n 5 -p PID    - Change priority
      
      Common Signals:
      • SIGTERM (15) - Graceful termination
      • SIGKILL (9)  - Force kill
      • SIGHUP (1)   - Reload configuration
    
    exercises:
      - id: ex_03
        type: command
        description: "Start sleep in background and list jobs"
        command: sleep 100 & jobs
        expected_pattern: "sleep"
        hints:
          - "Use: sleep 100 &"
          - "Then: jobs"
          - "Or combine: sleep 100 & jobs"
        points: 20

  - id: lesson_04
    title: "System Monitoring"
    notes: |
      📊 System Resource Monitoring
      
      uptime - System uptime and load:
      ───────────────────────────────
      • uptime            - Show how long system is running
      
      free - Memory usage:
      ───────────────────
      • free              - Show memory stats
      • free -h           - Human-readable format
      • free -m           - Show in megabytes
      
      df - Disk space:
      ───────────────
      • df                - Show disk usage
      • df -h             - Human-readable
      • df -i             - Show inode usage
      
      du - Directory size:
      ───────────────────
      • du -sh directory  - Size of directory
      • du -h --max-depth=1  - Size of subdirectories
      
      iostat - I/O statistics (if installed)
      vmstat - Virtual memory stats
    
    exercises:
      - id: ex_04
        type: command
        description: "Check system uptime"
        command: uptime
        expected_pattern: "up"
        hints:
          - "Just type: uptime"
        points: 10
      
      - id: ex_05
        type: command
        description: "Check memory usage in human-readable format"
        command: free -h
        expected_pattern: "Mem:"
        hints:
          - "Use: free -h"
        points: 15

completion_criteria:
  min_exercises_completed: 4
  min_score: 60
