id: package_management_04
level: intermediate
title: "Package Management"
description: "Install, update, and manage software packages"
estimated_time: 30

lessons:
  - id: lesson_01
    title: "APT - Debian/Ubuntu"
    notes: |
      📦 APT Package Manager (Debian/Ubuntu)
      
      Common Commands:
      ───────────────
      • apt update            - Update package list
      • apt upgrade           - Upgrade all packages
      • apt install package   - Install package
      • apt remove package    - Remove package
      • apt purge package     - Remove package + config
      • apt search keyword    - Search for packages
      • apt show package      - Show package info
      • apt list --installed  - List installed packages
      • apt autoremove        - Remove unused packages
      
      Examples:
      • sudo apt update
      • sudo apt install nginx
      • sudo apt remove apache2
      • apt search "web server"
      
      Note: Most commands need sudo!
    
    exercises:
      - id: ex_01
        type: command
        description: "Search for packages containing 'curl'"
        command: "apt search curl 2>/dev/null | head -5"
        expected_pattern: "curl"
        hints:
          - "Use: apt search curl"
        points: 15
      
      - id: ex_02
        type: command
        description: "Show information about the 'bash' package"
        command: "apt show bash 2>/dev/null | head -10"
        expected_pattern: "Package: bash"
        hints:
          - "Use: apt show bash"
        points: 15

  - id: lesson_02
    title: "YUM/DNF - RedHat/CentOS"
    notes: |
      📦 YUM/DNF Package Manager (RedHat/CentOS/Fedora)
      
      DNF (newer) / YUM (older):
      ─────────────────────────
      • dnf update            - Update package list
      • dnf upgrade           - Upgrade packages
      • dnf install package   - Install package
      • dnf remove package    - Remove package
      • dnf search keyword    - Search packages
      • dnf info package      - Package information
      • dnf list installed    - List installed
      • dnf clean all         - Clean cache
      
      YUM commands are similar:
      • yum install package
      • yum remove package
      • yum search keyword
      
      Note: DNF is the modern replacement for YUM
    
    exercises:
      - id: ex_03
        type: question
        question: "Which command updates the package list on Ubuntu?"
        options:
          - "apt update"
          - "apt upgrade"
          - "apt install"
          - "apt search"
        correct_answer: "apt update"
        points: 10

  - id: lesson_03
    title: "System Services"
    notes: |
      🔧 systemctl - Service Management
      
      Managing Services:
      ─────────────────
      • systemctl start service    - Start service
      • systemctl stop service     - Stop service
      • systemctl restart service  - Restart service
      • systemctl reload service   - Reload config
      • systemctl status service   - Check status
      • systemctl enable service   - Auto-start on boot
      • systemctl disable service  - Disable auto-start
      • systemctl is-active service - Check if running
      • systemctl is-enabled service - Check if enabled
      
      Listing Services:
      ────────────────
      • systemctl list-units --type=service
      • systemctl list-unit-files --type=service
      
      Examples:
      • sudo systemctl start nginx
      • sudo systemctl enable ssh
      • systemctl status apache2
    
    exercises:
      - id: ex_04
        type: command
        description: "Check if ssh service is active"
        command: "systemctl is-active ssh || systemctl is-active sshd"
        hints:
          - "Use: systemctl is-active ssh"
          - "On some systems it's 'sshd'"
        points: 15

completion_criteria:
  min_exercises_completed: 3
  min_score: 40
