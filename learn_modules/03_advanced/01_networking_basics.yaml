id: networking_basics_01
level: advanced
title: "Networking Fundamentals"
description: "Master network configuration and troubleshooting commands"
estimated_time: 45

lessons:
  - id: lesson_01
    title: "Network Interface Configuration"
    notes: |
      🌐 Network Interface Commands
      
      ip - Modern network configuration:
      ──────────────────────────────
      • ip addr show              - Show all interfaces
      • ip addr show eth0         - Show specific interface
      • ip link set eth0 up       - Bring interface up
      • ip link set eth0 down     - Bring interface down
      • ip addr add 192.168.1.10/24 dev eth0  - Add IP
      • ip route show             - Show routing table
      • ip route add default via 192.168.1.1  - Add route
      
      ifconfig (older, still common):
      ──────────────────────────────
      • ifconfig                  - Show all interfaces
      • ifconfig eth0             - Show specific interface
      • ifconfig eth0 up          - Bring up
      • ifconfig eth0 192.168.1.10 netmask 255.255.255.0
      
      Examples:
      • ip addr show
      • ip link show
      • ip route show
    
    exercises:
      - id: ex_01
        type: command
        description: "Show all network interfaces"
        command: ip addr show
        expected_pattern: "inet"
        hints:
          - "Use: ip addr show"
          - "Or shorter: ip a"
        points: 15
      
      - id: ex_02
        type: command
        description: "Display routing table"
        command: ip route show
        expected_pattern: "default|via"
        hints:
          - "Use: ip route show"
          - "Or: ip r"
        points: 15

  - id: lesson_02
    title: "Network Diagnostics"
    notes: |
      🔍 Network Troubleshooting Tools
      
      ping - Test connectivity:
      ────────────────────────
      • ping host             - Ping continuously
      • ping -c 4 host        - Ping 4 times
      • ping -i 2 host        - 2 second interval
      
      traceroute/tracepath - Trace route:
      ──────────────────────────────────
      • traceroute google.com
      • tracepath google.com
      
      netstat - Network statistics:
      ────────────────────────────
      • netstat -tuln         - Show listening ports
      • netstat -r            - Show routing table
      • netstat -i            - Show interfaces
      
      ss - Socket statistics (modern):
      ───────────────────────────────
      • ss -tuln              - Listening TCP/UDP
      • ss -s                 - Summary
      • ss -p                 - Show processes
      
      nslookup/dig - DNS lookup:
      ─────────────────────────
      • nslookup google.com
      • dig google.com
      • dig @8.8.8.8 google.com
    
    exercises:
      - id: ex_03
        type: command
        description: "Ping localhost 3 times"
        command: ping -c 3 127.0.0.1
        expected_pattern: "3 packets transmitted"
        hints:
          - "Use: ping -c 3 127.0.0.1"
        points: 15
      
      - id: ex_04
        type: command
        description: "Show all listening ports"
        command: ss -tuln
        expected_pattern: "LISTEN|State"
        hints:
          - "Use: ss -tuln"
          - "Or netstat -tuln if ss not available"
        points: 20

  - id: lesson_03
    title: "Firewall Basics"
    notes: |
      🔥 Firewall Management
      
      iptables (traditional):
      ──────────────────────
      • iptables -L           - List rules
      • iptables -L -n        - List (numeric)
      • iptables -A INPUT -p tcp --dport 80 -j ACCEPT
      • iptables -D INPUT 1   - Delete rule
      • iptables -F           - Flush all rules
      
      firewalld (modern):
      ──────────────────
      • firewall-cmd --state
      • firewall-cmd --list-all
      • firewall-cmd --add-port=80/tcp
      • firewall-cmd --remove-port=80/tcp
      • firewall-cmd --reload
      
      ufw (Ubuntu):
      ────────────
      • ufw status
      • ufw enable
      • ufw allow 22/tcp
      • ufw deny 80/tcp
      • ufw delete allow 22
    
    exercises:
      - id: ex_05
        type: command
        description: "List current iptables rules"
        command: sudo iptables -L -n || iptables -L -n 2>/dev/null || echo "iptables not available"
        hints:
          - "Use: sudo iptables -L -n"
        points: 20

completion_criteria:
  min_exercises_completed: 4
  min_score: 65
